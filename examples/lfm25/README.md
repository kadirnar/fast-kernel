# LFM2.5 campaign

`fast-kernel probe && fast-kernel baseline && fast-kernel dashboard`, then type "Optimize the LFM2.5 model." in Claude Code.
Use `fast-kernel init lfm25 --set model_args.variant=350m` for a quicker iteration loop on the same architecture.
