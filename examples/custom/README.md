# Custom campaign

1. Edit `spec.py`: `build_model()` and (optionally) the `Spec` class.
2. `fast-kernel baseline`, `fast-kernel dashboard`, then type "Optimize the PyTorch model in ./spec.py." in Claude Code.
