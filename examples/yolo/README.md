# YOLO campaign

Requires `uv sync --extra yolo` (ultralytics; weights download on first use). Then
`fast-kernel probe && fast-kernel baseline && fast-kernel dashboard` and type "Optimize the YOLO model." in Claude Code.
