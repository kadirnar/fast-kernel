# LFM2-Audio campaign

Requires `uv sync --extra audio` (liquid-audio). Then `fast-kernel probe && fast-kernel baseline && fast-kernel dashboard`
and type "Optimize the LFM2 audio model." in Claude Code.
